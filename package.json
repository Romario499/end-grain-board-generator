{
  "name": "torets-end-grain-board-generator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/**/*.test.js",
    "check": "node --test tests/**/*.test.js && find src tests -name '*.js' -print0 | xargs -0 -n1 node --check",
    "serve": "python3 -m http.server 8080"
  },
  "dependencies": {},
  "devDependencies": {}
}
