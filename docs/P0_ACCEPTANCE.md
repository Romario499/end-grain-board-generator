# P0 Acceptance

Дата проверки: 2026-08-13.

## Автоматический гейт

- [x] 53 теста проходят.
- [x] `npm run check` проходит.
- [x] Модель, валидация, трансформации, команды, хранилище, контроллер, рендеринг, pointer geometry и PNG покрыты.
- [x] Отдельный controller-тест подтверждает, что название и размеры сохраняются, открываются и передаются в экспорт без потерь.
- [x] RED зафиксирован перед каждым новым модулем.

## Визуальный smoke

- [x] Страница открыта в Microsoft Edge 1440 × 1000 через 127.0.0.1.
- [x] Видны три материала, Canvas, размеры, сетка, кнопки трансформаций и статус.
- [x] Трансформации отключены при пустом выборе.
- [x] В интерактивном браузере подтверждены: выбор материала, покраска ячейки, выбор одной ячейки и доступность трансформаций.
- [x] Сохранённый проект появляется в списке и открывается через интерфейс.
- [x] В консоли интерактивного smoke нет предупреждений и ошибок.
- [x] UI не загружает внешние шрифты, текстуры или данные.
- [ ] Автоматизация не генерирует надёжный `change/blur` для полей названия и размера; этот контракт подтверждён controller-тестом, но не полноценным браузерным вводом.
- [ ] Событие скачивания PNG не было перехвачено браузерным инструментом; формирование имени, размера и полный render path проверены автоматически.

## Нагрузка 64 × 64

- [x] 4096 ячеек создаются за 2.04 мс.
- [x] Типовое изменение занимает 0.67 мс.
- [x] Render plan создаётся за 2.80 мс.
- [x] Проход PNG через fake Canvas занимает 25.94 мс.
- [x] Save занимает 11.59 мс, load — 5.39 мс.

## Границы

- [x] P1, P2 и P3 не реализованы.
- [x] `../kalkulyator`, `.env`, ключи и внешние интеграции не затронуты.
- [x] Зависимости не устанавливались.

## Остаточные риски

- Браузерный инструмент не смог надёжно воспроизвести `change/blur` и перехват Blob-download; соответствующие контракты закрыты unit/integration тестами, но остаются пунктами ручной приёмки.
- Время реального Canvas PNG зависит от браузера и GPU; доменный размер и полный render path проверены.
