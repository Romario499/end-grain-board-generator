# Implementation Plan P0

Порядок: доменная модель → трансформации → команды редактора → рендерер → хранилище → контроллер → UI → сохранение/открытие → PNG → P0 acceptance gate. Каждый этап выполняется через RED/GREEN/REFACTOR и полный `npm run check`.
